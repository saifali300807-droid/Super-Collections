# Dependencies
node_modules/

# Environment secrets — GitHub par kabhi push mat karna!
server/.env
client/.env
.env
*.env

# Builds
client/dist/

# Logs
server/server.log
server/server-err.log
*.log

# Uploaded files — RENDER PAR NEEDED (ephemeral storage fix):
# Inhe git mein commit karo taaki Render deploy par images/videos available rahen.
# New admin uploads commit karne ke liye: git add server/src/uploads && commit

# OS / Editor junk
.DS_Store
Thumbs.db
.vscode/
